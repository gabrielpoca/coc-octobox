{
  "name": "coc-octobox",
  "version": "1.0.0",
  "description": "",
  "main": "./src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "engines": {
    "coc": "^0.0.46"
  },
  "activationEvents": [
    "*"
  ],
  "contributes": {
    "commands": [],
    "configuration": {
      "title": "octobox",
      "properties": {
        "octobox.iconForStatusBar": {
          "type": "string",
          "default": "♢",
          "description": "The icon to use next to the status bar",
          "scope": "resource"
        },
        "octobox.showInStatusBar": {
          "type": "boolean",
          "default": false,
          "description": "Show in status bar",
          "scope": "resource"
        }
      }
    }
  },
  "dependencies": {
    "axios": "^0.21.1",
    "coc.nvim": "^0.0.73"
  },
  "devDependencies": {
    "jasmine": "^3.4.0"
  }
}
